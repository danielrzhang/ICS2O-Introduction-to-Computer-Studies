while (userInputLength > 0) and (userInputWidth > 0):
    print(userInputLength * userInputWidth)
    userInputLength = float(input("Enter another length of a rectangle here: "))
    userInputWidth = float(input("Enter another width of a rectangle here: "))