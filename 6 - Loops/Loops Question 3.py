from math import sqrt
userInputNumber = float(input("Enter a number here: "))
while userInputNumber >=0:
    print(sqrt(userInputNumber))
    userInputNumber = float(input("Enter another number here: "))